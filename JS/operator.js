// 3. Operators
// Arithmetic: +, -, *, /, %, **
// Assignment: =, +=, -=, etc.
// Comparison: ==, ===, !=, <, >
// Logical: &&, ||, !
// Ternary operator condition ? true : false

let marks = 85;
let result = marks >= 35 ? "Pass" : "Fail";
console.log(result);