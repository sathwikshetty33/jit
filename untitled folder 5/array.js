// 7. Arrays
// Creating arrays
// Accessing elements
// Array methods:
// push(), pop(), shift(), unshift()
// forEach(), map(), filter(), reduce()


let numbers = [1, 2, 3, 4];
numbers.push(5);
console.log(numbers.map(n => n * 2)); // [2,4,6,8,10]
